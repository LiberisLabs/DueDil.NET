  branches:
    only:
      - master
   
  version: 0.0.0.{build}
   

  assembly_info:
    assembly_version: '{version}'
    assembly_file_version: '{version}'
    assembly_informational_version: '{version}'
    file: AssemblyInfo.*
    patch: true
   
  build:
    parallel: true

  configuration: Release
   
  cache:
    - packages -> **\packages.config
    - C:\downloads -> appveyor.yml
   
  test:
    assemblies:
      - '**\*.Tests.dll'
      - '**\*.FunctionalTests.dll'
         
  artifacts:
    - path: '*.nupkg'
     
  deploy:
    - provider: Environment
      name: MyGet Consumables
      

   
  notifications:
    - provider: Slack
      auth_token:
        secure: xoxp-8891918677-8891674647-9405162163-b18ee9
      channel: appveyor

  branches:
    except:
      - master

  version: 0.0.0.{build}-{branch}
    
  assembly_info:
    assembly_version: '{version}'
    assembly_file_version: '{version}'
    assembly_informational_version: '{version}'
    file: AssemblyInfo.*
    patch: true
   
  build:
    parallel: true
    verbosity: normal
   
  configuration: Release
   
  cache:
    - packages -> **\packages.config
    - C:\downloads -> appveyor.yml
   
  test:
    assemblies:
      - '**\*.Tests.dll'
      - '**\*.FunctionalTests.dll'

  artifacts:
    - path: '*.nupkg'
